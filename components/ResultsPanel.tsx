'use client';

import type { SimulationResult, Crypto } from '@/lib/types';

interface Props {
  result: SimulationResult;
  crypto: Crypto;
  frequency: string;
  amountPerPeriod: number;
}

export default function ResultsPanel(_props: Props) {
  return null;
}
